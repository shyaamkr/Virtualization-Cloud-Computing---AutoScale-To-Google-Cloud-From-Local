# Virtualization-Cloud-Computing---AutoScale-To-Google-Cloud-From-Local
Create a local VM and implement a mechanism to monitor resource usage. Configure it to auto-scale to a public cloud (e.g., Google Cloud) when resource usage exceeds 75%.
